---
paths:
  - 'database/migrations/**'
---

# Migrations

## Generate migrations through Artisan CLI
Always create migrations using `php artisan make:migration ...` so Laravel assigns proper timestamps. Do not create migration files manually.
